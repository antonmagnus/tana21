f = @(x) 8*x.^3 - 4*x.^2 + 6*x - 3;
g = @(x) sqrt(x);
a = 1;
b = 2;
n = 4;
[S, x] = SimpsonsRule(g, a, b, n);



loop = [2,4,8,16];

for i = 1:
I_True = integral(g,a,b);

%syms x;
%g = 8*x^3 - 4*x^2 + 6*x - 3;

%I = int(g, a, b);

err = abs(I_True - S)